export interface ApplicationState {
  subNavigator: boolean
  navigator: boolean
}

/**
 * navigator
 */
export const applicationState: ApplicationState = {
  subNavigator: true,
  navigator: true,
}
